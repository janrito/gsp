require(['config'], function (config) {

  // Main
  require([
    'jquery',
    'visitors',
    // 'bootstrap/transition',
    // 'bootstrap/collapse',
    // 'bootstrap/tooltip',
    ], function ($) {

      $( document ).ready(function() {

        console.log('hello');

      });
  });

});